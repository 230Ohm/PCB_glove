# Proposal 015I read-only capture 1 — not acquired

No STM32N6570-DK or ST-LINK was visible and STM32CubeProgrammer was unavailable. This directory intentionally contains no invented log, screenshot, option-byte value, OTP value, lifecycle state, device ID, firmware vector or hash.

When hardware is available, preserve the existing image first, execute only the documented read-only capture procedure from Proposal 015H, store raw outputs here, hash every file, disconnect, and obtain capture 2 independently before any flash write.
